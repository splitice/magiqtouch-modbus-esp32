# Rename this file to secrets.yaml and fill in your actual credentials
# DO NOT commit secrets.yaml to version control!

# WiFi credentials
wifi_ssid: "YourWiFiSSID"
wifi_password: "YourWiFiPassword"

# Fallback AP credentials (when WiFi fails to connect)
fallback_password: "magiqtouch123"

# API encryption key for Home Assistant
# Generate with: openssl rand -base64 32
api_encryption_key: "your-32-character-base64-key-here=="

# OTA update password
ota_password: "your-ota-password-here"
